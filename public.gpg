# Placeholder — replace with your exported public key after Phase 4.
#
# Run:
#   gpg --armor --export packages@protibimbok.com > public.gpg
#
# Until then, apt installs can use [trusted=yes] as a temporary workaround.
